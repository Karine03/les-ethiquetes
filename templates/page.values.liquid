{%- assign force_secondary_background = true -%}

<div class="page__content-wrapper value-page">
  <div class="container">
    <div class="page__header">
      <h1 class="page__title h1">{{ page.title }}</h1>
      <p> {{ page.content }} </p>
    </div>
  </div>
</div>

{% section 'list-values-template' %}